<li class="list-group-item">
  <span class="glyphicon glyphicon-plus"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <% if a.trackable %>
    <%= a.trackable.medname[0] %> (<%= a.trackable.inventoryid %>) was dispensed to <%= a.trackable.firstname[0] %> <%= a.trackable.lastname[0] %> (<%= a.trackable.patientsid %>).
  <% else %>
    A dispensal that is currently deleted was added.
  <% end %>
</li>