<li class="list-group-item">
  <span class="glyphicon glyphicon-plus"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <% if a.trackable %>
    <%= a.trackable.medname[0] %> (<%= a.trackable.invid %>) was added to the <%= a.trackable.inventorytype %> inventory. The date to reorder is <%= a.trackable.datetoreorder %>.
  <% else %>
    A dispensal that is currently deleted was added.
  <% end %>
</li>