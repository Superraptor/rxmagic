<li class="list-group-item">
  <span class="glyphicon glyphicon-edit"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <% if a.trackable %>
    <%= a.trackable.medname[0] %> (<%= a.trackable.invid %>) in the <%= a.trackable.inventorytype %> inventory was updated. The date to reorder is <%= a.trackable.datetoreorder %>.
  <% else %>
    An inventory that is currently deleted was edited.
  <% end %>
</li>