<li class="list-group-item">
  <span class="glyphicon glyphicon-repeat"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <% if a.trackable %>
    <%= a.trackable.medname[0] %> needs to be reordered. Current stock is <%= a.trackable.currentstock %>. Minimum stock is <%= a.trackable.minstock[0] %>.
  <% else %>
    An inventory that was deleted needed to be reordered.
  <% end %>
</li>